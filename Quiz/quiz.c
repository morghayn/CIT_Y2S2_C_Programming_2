#include "quiz.h"

void create_interchange()
{
	printf("hmmm");
}
	//struct interchange* temp;
	/*
	temp = (interchange*) malloc(sizeof(interchange));

	char* question_copy = (char*)malloc((strlen(question) + 1) * sizeof(char));
	char* answer_copy = (char*)malloc((strlen(answer) + 1) * sizeof(char));

	strcpy(question_copy, question);
	strcpy(answer_copy, answer);

	temp->question = question_copy;
	temp->answer = answer_copy;
	*/
	//return temp;

/*
int length(node* head)
{
	int length = 0;

	while (head != NULL)
	{
		length++;
		head = head->next;
	}

	return length;
}

void release_nodes(node* head)
{
	node* temp;

	while (head != NULL)
	{
		temp = head;
		head = head->next;
		free(temp->question);
		free(temp->answer);
		free(temp);
	}
}
*/